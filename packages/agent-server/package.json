{
  "name": "@nav/agent-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm run build && node --watch dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@ai-sdk/harness": "1.0.5",
    "@ai-sdk/harness-pi": "1.0.5",
    "@ai-sdk/sandbox-just-bash": "1.0.5",
    "ai": "7.0.3",
    "just-bash": "^2.14.5"
  },
  "devDependencies": {
    "@types/node": "26.0.1",
    "typescript": "6.0.3"
  }
}
