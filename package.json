{
  "name": "nav",
  "private": true,
  "description": "Learning project for building a coding agent.",
  "scripts": {
    "electron:dev": "electron desktop/electron/main.cjs",
    "electron:smoke": "electron desktop/electron/main.cjs --smoke",
    "check:electron": "node --test tests/*.test.cjs",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "electron": "^42.2.0"
  },
  "dependencies": {
    "dompurify": "^3.4.5",
    "marked": "^18.0.4"
  }
}
